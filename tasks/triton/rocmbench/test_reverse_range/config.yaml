compile_command:
- python -c "import ast; ast.parse(open('test_reverse_range.py').read())"
correctness_command:
- pytest -vv -x --maxfail=1 test_reverse_range.py -k "not test_performance and not
  test_save_performance_results"
performance_command:
- pytest -vv -x --maxfail=1 test_reverse_range.py -k "test_performance or test_save_performance_results"
task_type: instruction2triton
task_result_template: null
prompt:
  cheatsheet: null
  instructions: "\nYou are an expert in triton programming language. You will be given\
    \ the function definition for the `reverse_range`. Your task is to complete the\
    \ kernel code. Only complete the kernel code in the function definition, DONT\
    \ remove any python imports or helper utils in the instruction/code provided,\
    \ DONT change/interfere with the provided function definition and parameter list.\n\
    \nThis kernel, `reverse_range`,  is designed to reverse a specific 512-element\
    \ segment from an input tensor and stores it into an output tensor.\n\n**Your\
    \ objective is to implement the body of `reverse_range`.**\n\nYou must ensure\
    \ that:\n1.  All arguments received by `reverse_range` are kept intact and not\
    \ modified.\n2. Provide you final code in ```python code block. \nExample:\n```python\n\
    <YOUR-CODE-HERE>\n```\nThe full definition for `reverse_range` and relevant helper\
    \ utilities are provided in the context below. You only need to complete the code\
    \ for `reverse_range` whilst keeping other things intact. DONT remove Imports\
    \ and HELPER utils.\n\n######################################## Imports ########################################\
    \ \nimport numpy as np\nimport pytest\nimport torch\nfrom numpy.random import\
    \ RandomState\n\nimport triton\nimport triton.language as tl\n########################################\
    \ Imports ######################################## \n\n@triton.jit\ndef reverse_range(in_ptr,\
    \ out_ptr):\n    \"\"\"\n    Reverses a specific 512-element segment from an input\
    \ tensor and stores it\n    into an output tensor.\n\n    This kernel operates\
    \ on fixed-size blocks of 512 elements.\n    It performs the following operation\
    \ for each element `i` in the range `[0, 511]`:\n    `out_ptr[i] = in_ptr[512\
    \ - i]`\n\n    This means the kernel reads elements from `in_ptr + 1` up to `in_ptr\
    \ + 512`\n    (inclusive) and writes them in reversed order to `out_ptr + 0` up\
    \ to\n    `out_ptr + 511` (inclusive).\n\n    Parameters\n    ----------\n   \
    \ in_ptr : tl.pointer_type\n        A pointer to the input tensor. The kernel\
    \ reads a block of 512 elements\n        from this tensor. Specifically, it reads\
    \ from memory locations\n        `in_ptr + 512` down to `in_ptr + 1`. For example,\
    \ the value at\n        `in_ptr + 512` is read first (for `x0=0` in the original\
    \ implementation)\n        and stored at `out_ptr + 0`.\n    out_ptr : tl.pointer_type\n\
    \        A pointer to the output tensor. The kernel writes the 512 reversed\n\
    \        elements to this tensor. Specifically, it writes to memory locations\n\
    \        `out_ptr + 0` through `out_ptr + 511`. For example, `out_ptr + 0`\n \
    \       receives the value from `in_ptr + 512`.\n    \"\"\"\n    # Your code here\n\
    \n"
  source_code: null
source_file_path:
- null
target_kernel_functions:
- reverse_range
